name: Test Artifact

on: [push]

jobs:
  test:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - run: echo "hello" > test.txt
      - uses: actions/upload-artifact@v2
        with:
          name: test-artifact
          path: test.txt
